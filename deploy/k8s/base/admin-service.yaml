apiVersion: v1
kind: Service
metadata:
  name: ghcp-admin
  labels:
    app.kubernetes.io/component: admin
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/component: admin
    app.kubernetes.io/part-of: ghcp-pool-proxy
  ports:
    - name: http
      port: 8001
      targetPort: http
      protocol: TCP
      appProtocol: http
